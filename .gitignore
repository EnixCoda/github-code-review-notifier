.vscode
.DS_Store
node_modules
now-secrets.json
build

.env
.vercel
.env*

# Yarn is the single source of truth for deps; the stale npm lockfile is not
# tracked so Vercel installs from yarn.lock.
package-lock.json
